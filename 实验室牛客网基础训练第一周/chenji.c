#include<stdio.h>
int main()
{
    int i;
    scanf("%d",i)
    if (i>=90&&i<=100)
    printf("%c",'A');
     if£¨i>=80&&i<=89)
    printf("%c",'B');
     if (i>=70&&i<=79)
    printf("%c",'C');
     if (i>=60&&i<=69)
     printf("%c",'D');
     if (i<=59)
     printf("%c",'E');
    if(i<0&&i>100)
     printf("%c",'F');
    return 0;
}
